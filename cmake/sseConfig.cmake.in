get_filename_component(sse_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${sse_CMAKE_DIR})

# find_dependency(OpenCascade REQUIRED)

if(NOT TARGET sse::sse)
    include("${sse_CMAKE_DIR}/sseTargets.cmake")
endif()

set(sse_LIBRARIES sse::sse)
